{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../dashboard && npm install --include=dev && npm run build && cd ../backend && npm install --include=dev",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}